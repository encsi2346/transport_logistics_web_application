/*export enum RequestStatus { //Kérés állapota
    DRAFT = 'draft',
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REFUSED = 'refused',
    DELETED = 'deleted',
}
*/
export const RequestStatus = Object.freeze({
    DRAFT: 'draft',
    PENDING: 'pending',
    ACCEPTED: 'accepted',
    REFUSED: 'refused',
    DELETED: 'deleted',
});