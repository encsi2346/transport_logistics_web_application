/*export enum RequestStatus { //Kérés állapota
    DRAFT = 'draft',
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REFUSED = 'refused',
    DELETED = 'deleted',
}
*/
const RequestStatus = Object.freeze({
    DRAFT: 'draft',
    PENDING: 'pending',
    ACCEPTED: 'accepted',
    REFUSED: 'refused',
    DELETED: 'deleted',
});