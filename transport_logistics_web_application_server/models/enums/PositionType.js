/*export enum PositionType { //Pozíció típusok
    DRIVER = 'driver',
    HR = 'hr', //TODO: kigyűjteni az összeset
}
*/
export const PositionType = Object.freeze({
    DRIVER: 'driver',
    HR: 'hr',
});